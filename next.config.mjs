/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
      domains: ["m.media-amazon.com","www.imdb.com"], // Add the domain here
    },
 

    
};

export default nextConfig;
